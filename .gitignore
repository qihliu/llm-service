# Python virtual environments — created locally, never committed
.venv/
venv/
__pycache__/
*.pyc

# HuggingFace model cache — models are downloaded at runtime, not stored in git
.cache/
*.bin
*.safetensors

# macOS metadata
.DS_Store
